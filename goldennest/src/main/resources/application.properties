spring.application.name=goldennest

# --- Database (MySQL on Railway, PUBLIC network) ---
spring.datasource.url=jdbc:mysql://interchange.proxy.rlwy.net:23365/railway?useUnicode=true&characterEncoding=utf8&connectionCollation=utf8mb4_unicode_ci&allowPublicKeyRetrieval=true&useSSL=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=anLbMNPIOJwxMmzeMWfKLAWbLAgVHXTJ
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# --- Flyway ---
spring.flyway.enabled=false
spring.flyway.baseline-on-migrate=true

# --- Server ---
server.port=${PORT:8080}

# --- Actuator / metrics ---
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.probes.enabled=true

# --- CORS (for your React frontend) ---
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000,https://thegoldennest.co.uk,https://golden-nest-main.vercel.app

# --- JWT settings ---
app.jwt.secret=${JWT_SECRET:Q3hR2KkGZp7a9Y8f2zE4rT6uV9xB1nC3dF5hJ7lM9pQ2sT4vX6zA8bC0dE2gH4}
app.jwt.access-minutes=60
app.jwt.refresh-days=14

# --- Google Maps ---
google.maps.api.key=${GOOGLE_MAPS_API_KEY:AIzaSyAW2ihE-P89mPgHySqVbm4eLdp7cp6hKek}

logging.level.org.springframework.security=DEBUG
